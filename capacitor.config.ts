import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'prt.kepazah.www',
  appName: 'PRT online',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
